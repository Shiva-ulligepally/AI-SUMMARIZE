{
  "name": "smartsumm-monorepo",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "24.x"
  },
  
  "scripts": {
    "build": "node build.js",
    "start": "node server/src/index.js",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
