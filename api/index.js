import app from '../backend/src/index.js';

// Express `app` is a valid handler function for Vercel's Node runtime.
// We simply export it as the default export so Vercel can invoke it for /api/* routes.

export default app;
